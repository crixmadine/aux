'use strict';

describe('auxApp.search module', function() {
  /*
  beforeEach(function() {
    angular.module('ngRoute',[]);

    module('auxApp.search');
  
  beforeEach(module('auxApp.search'));
  */

  /*
  describe('search controller', function() {
    var $scope, searchCtrl, releaseSrv;

    beforeEach(inject(function($rootScope, $controller, releaseService) {
      $scope = $rootScope.$new();
      searchCtrl = $controller;
      releaseSrv = releaseService;
    }));

    it('should be defined', inject(function() {
      //spec body
      searchCtrl('SearchCtrl', {$scope: $scope});
      expect(searchCtrl).toBeDefined();
    }));

  });
  */
});
